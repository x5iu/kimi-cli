# Example: Custom Echo AgentLoop

This example demonstrates how to write a custom `AgentLoop` (agent loop) implementation that can be used with Kimi Code CLI's `Shell` UI.

```sh
cd examples/custom-echo-agent-loop
uv sync --reinstall
uv run main.py
```
