# Example: Custom Kimi AgentLoop

This example demonstrates how to extend the `KimiAgentLoop` (builtin agent loop) to customize its behavior and use it with the `Shell` UI.

```sh
cd examples/custom-kimi-agent-loop
uv sync --reinstall
uv run main.py
```
