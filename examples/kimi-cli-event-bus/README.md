# Example: Kimi Code CLI EventBus Messages

This example demonstrates how to create and run a Kimi Code CLI instance with raw EventBus message output.

```sh
cd examples/kimi-cli-event-bus
uv sync --reinstall
uv run main.py
```
